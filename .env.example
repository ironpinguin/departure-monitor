# Docker Compose Environment Variables for departure-monitor

# GitHub Container Registry Configuration
IMAGE_TAG=latest

# Host Configuration
HOST_PORT=8080

# Example usage:
# 1. Copy this file to .env: cp .env.example .env
# 2. Update the values according to your setup
# 3. Run: docker-compose -f docker-compose.prod.yml up -d

# Available image tags:
# - latest (newest release)
# - v1.0.0 (specific version)
# - main (latest main branch build)
# - develop (latest develop branch build)

# Environment Variablen for MCP Server
GITHUB_PERSONAL_ACCESS_TOKEN=add your token
BRAVE_API_KEY=add your api key